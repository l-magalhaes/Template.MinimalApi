# 🧱 Template.MinimalApi (.NET 9 + SQL Server + Docker)

Projeto de exemplo em .NET 9 usando Minimal API, Entity Framework Core e SQL Server, rodando via Docker Compose.

---

## 🚀 Subir o projeto com Docker

1. Certifique-se de ter o **Docker** instalado e em execução.  
   👉 [Baixar Docker Desktop](https://www.docker.com/products/docker-desktop/)

2. No terminal, dentro da pasta do projeto (onde está o `docker-compose.yml`), execute:

   ```bash
   docker compose up --build